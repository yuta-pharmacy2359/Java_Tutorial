package exercise2;

public class Adder extends SuperAdder {
	/*  public void add(int n) {
			number += n;;
		} */
	public void add() {
	}
}

// 引数の構成が抽象クラス(SuperAdderクラス)のaddメソッドと一致していない(オーバーライドされていない)