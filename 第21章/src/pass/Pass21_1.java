package pass;

public class Pass21_1 {
	/*

	 抽象クラスについて正しいもの
	 A. 継承するサブクラスは、すべての抽象メソッドをオーバーライドしなければならない。→ x (サブクラスも抽象クラスにすれば、抽象メソッドを実装しなくてもよい)
	 B. コンストラクタにもabstract修飾子が必要である。→ x
	 C. 抽象クラス型のオブジェクトを作成できる。→ x (抽象クラスは定義が不完全なので、インスタンス(具体的な値を持ったオブジェクト)を作成できない。)
	 D. 抽象クラス型の変数にオブジェクトを代入してもよい。→ ◯
	 E. フィールド変数をabstractにできる。→ x
	 F. 抽象メソッドは、public void abstract method();のように定義する。→ x (void method();のように定義する。)

	 */
}
